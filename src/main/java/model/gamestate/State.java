package model.gamestate;

/**
 * Class for storing the GameState Enum
 */
public class State {

    /**
     * Enum that will represent the game states
     */
    public enum GameState {
        START,
        RESTART,
        NONE,
        PAUSED
    }

}
